# Core dependencies 
pandas>=2.0.0 
tabulate==0.9.0 
 
# Database (PostgreSQL for cloud) 
psycopg2-binary>=2.9.0 
 
# Web framework (for browser interface) 
flask>=3.0.0 
gunicorn>=21.0.0 
 
# Excel export 
openpyxl>=3.1.0
python-dotenv>=1.0.0

#for CSRF protection
flask-wtf>=1.2.0

# ADDED: rate limiting to prevent brute-force attacks on Basic Auth endpoints
flask-limiter>=3.5.0

# Tests (pytest suite under test_scripts/; run: python -m pytest test_scripts -q)
pytest>=8.0.0
pytest-cov>=5.0.0
# property-based tests for the unit-conversion layer (test_units.py)
hypothesis>=6.0.0