# Pinned to exact versions (was >= ranges) so a Render rebuild over the summer can't silently
# pull a breaking upgrade. To intentionally bump: change the version here, run the test suite
# (python -m pytest test_scripts -q), then redeploy. Last verified set: 2026-06-29.

# Core dependencies
pandas==2.3.3
tabulate==0.9.0

# IANA timezone database for zoneinfo (config.py's business_today()/business_now()). Linux
# containers usually ship a system tz database and Windows never does — pin explicitly so
# BUSINESS_TZ can't silently break depending on the host.
tzdata==2025.3

# Database (PostgreSQL for cloud)
psycopg2-binary==2.9.11

# Web framework (for browser interface)
flask==3.1.2
werkzeug==3.1.5          # Flask's WSGI dependency — pinned to keep it compatible with Flask 3.1.x
gunicorn==25.0.1

# Excel export
openpyxl==3.1.5
python-dotenv==1.2.1

# CSRF protection
flask-wtf==1.2.2

# Rate limiting to prevent brute-force attacks on Basic Auth endpoints
flask-limiter==4.1.1

# Tests (pytest suite under test_scripts/; run: python -m pytest test_scripts -q)
pytest==9.1.1
pytest-cov==7.1.0
# property-based tests for the unit-conversion layer (test_units.py)
hypothesis==6.155.7
